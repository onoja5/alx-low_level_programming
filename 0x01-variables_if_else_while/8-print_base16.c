#include <stdio.h>


/**
 * main - this prints all numbers of base 64 in lowercase
 *
 * Return: 0 Always
 */
int main(void)
{
	char c;
	int i;

	for (i = 0; i < 10; i++)
	{
		putchar(i + '0');
	}
	for (c = 'a'; c < 'g'; c++)
	{
		putchar(c);
	}
	putchar('\n');

	return (0);
}
