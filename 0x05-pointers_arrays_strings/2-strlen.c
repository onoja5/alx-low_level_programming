#include "main.h"

/**
 * _strlen - This functions return the length of the string
 * @s: char type pointer
 * Return: length of string
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\n'; ++i)
	;
	return (c);
}
